[profile.default]
# Kill individual tests after 60 seconds to prevent slow bootstrap-heavy
# tests from blocking the entire suite within a 10-minute overall timeout.
slow-timeout = { period = "30s", terminate-after = 2 }
