; read-event with non-nil SECONDS does not seed this-command-keys* when empty.
; once key context exists, later non-nil SECONDS reads preserve that context.

(this-command-keys-vector)

(let ((unread-command-events (list 97)))
  (read-event nil nil 0))
(this-command-keys-vector)

(let ((unread-command-events (list 98)))
  (read-event nil nil 1))
(this-command-keys-vector)

(let ((unread-command-events (list 99)))
  (read-event nil nil 0.25))
(this-command-keys-vector)

(let ((unread-command-events (list 100)))
  (read-event nil nil nil))
(this-command-keys-vector)

(let ((unread-command-events (list '(mouse-1))))
  (read-event nil nil 0))
(this-command-keys-vector)

(let ((unread-command-events nil))
  (read-event nil nil 1))
(this-command-keys-vector)
