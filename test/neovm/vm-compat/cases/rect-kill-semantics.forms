(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (kill-rectangle 1 9) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (list (kill-rectangle 20 1) (string-to-list (buffer-string))))
(condition-case err (kill-rectangle "x" 2) (error (car err)))
(condition-case err (kill-rectangle 1 "x") (error (car err)))
