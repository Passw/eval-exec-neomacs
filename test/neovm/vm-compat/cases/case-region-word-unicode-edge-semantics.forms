;; Lock-in: Unicode edge behavior for region/word case commands.
(list
 (list 'downcase-region-kelvin
       (with-temp-buffer
         (insert "K")
         (downcase-region (point-min) (point-max))
         (buffer-string)))
 (list 'downcase-region-a7cb
       (with-temp-buffer
         (insert (char-to-string 42955))
         (downcase-region (point-min) (point-max))
         (buffer-string)))
 (list 'upcase-region-dotless-i
       (with-temp-buffer
         (insert "ı")
         (upcase-region (point-min) (point-max))
         (buffer-string)))
 (list 'upcase-region-a7cd
       (with-temp-buffer
         (insert (char-to-string 42957))
         (upcase-region (point-min) (point-max))
         (buffer-string)))
 (list 'upcase-region-sharp-s
       (with-temp-buffer
         (insert "ß")
         (upcase-region (point-min) (point-max))
         (buffer-string)))
 (list 'capitalize-region-sharp-s
       (with-temp-buffer
         (insert "ß")
         (capitalize-region (point-min) (point-max))
         (buffer-string)))
 (list 'upcase-initials-region-sharp-s
       (with-temp-buffer
         (insert "ß")
         (upcase-initials-region (point-min) (point-max))
         (buffer-string)))
 (list 'capitalize-region-armenian-ligature
       (with-temp-buffer
         (insert (char-to-string 1415))
         (capitalize-region (point-min) (point-max))
         (buffer-string)))
 (list 'upcase-initials-region-armenian-ligature
       (with-temp-buffer
         (insert (char-to-string 1415))
         (upcase-initials-region (point-min) (point-max))
         (buffer-string)))
 (list 'capitalize-region-latin-ligature-ff
       (with-temp-buffer
         (insert (char-to-string 64256))
         (capitalize-region (point-min) (point-max))
         (buffer-string)))
 (list 'downcase-word-kelvin
       (with-temp-buffer
         (insert "K")
         (goto-char (point-min))
         (downcase-word 1)
         (buffer-string)))
 (list 'downcase-word-10d50
       (with-temp-buffer
         (insert (char-to-string 68944))
         (goto-char (point-min))
         (downcase-word 1)
         (buffer-string)))
 (list 'upcase-word-dotless-i
       (with-temp-buffer
         (insert "ı")
         (goto-char (point-min))
         (upcase-word 1)
         (buffer-string)))
 (list 'upcase-word-10d70
       (with-temp-buffer
         (insert (char-to-string 68976))
         (goto-char (point-min))
         (upcase-word 1)
         (buffer-string)))
 (list 'upcase-word-sharp-s
       (with-temp-buffer
         (insert "ß")
         (goto-char (point-min))
         (upcase-word 1)
         (buffer-string)))
 (list 'capitalize-word-greek-iota-subscript
       (with-temp-buffer
         (insert (char-to-string 8114))
         (goto-char (point-min))
         (capitalize-word 1)
         (buffer-string))))
