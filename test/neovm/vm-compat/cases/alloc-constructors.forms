; alloc.c constructors
(make-vector 3 0)
(make-string 5 ?x)
(make-list 3 nil)
(vector 1 2 3)
(cons 1 2)
(list 1 2 3)

; alloc.c symbol creation
(symbolp (make-symbol "fresh"))
(symbol-name (make-symbol "fresh"))

; alloc.c garbage-collect returns without error
(listp (garbage-collect))

; alloc.c length and type checks
(length (make-vector 5 0))
(length (make-string 3 ?a))
(length (make-list 4 nil))
