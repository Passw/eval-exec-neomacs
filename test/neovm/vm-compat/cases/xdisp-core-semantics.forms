; xdisp.c — display operations
; format-mode-line
(stringp (format-mode-line "test"))
(equal (format-mode-line "hello") "hello")
(stringp (format-mode-line "%b"))

; invisible-p
(eq (invisible-p nil) nil)
(with-temp-buffer
  (insert "hello")
  (put-text-property 1 6 'invisible t)
  (eq (invisible-p t) t))

; current-bidi-paragraph-direction
(with-temp-buffer
  (insert "hello")
  (symbolp (current-bidi-paragraph-direction)))

; line-pixel-height
(with-temp-buffer
  (insert "hello")
  (integerp (line-pixel-height)))

; lookup-image-map
(eq (lookup-image-map nil nil) nil)
