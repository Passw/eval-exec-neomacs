(let ((unread-command-events (list [foo] [bar])))
  (progn
    (read-key "")
    unread-command-events))

(let ((unread-command-events '([foo] . [bar])))
  (progn
    (read-key "")
    unread-command-events))
