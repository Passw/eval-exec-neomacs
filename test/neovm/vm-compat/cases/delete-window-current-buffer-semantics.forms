(save-current-buffer (let* ((b1 (get-buffer-create "dw-curbuf-a")) (b2 (get-buffer-create "dw-curbuf-b"))) (set-window-buffer nil b1) (let ((w2 (split-window))) (set-window-buffer w2 b2) (select-window w2) (delete-window w2) (buffer-name (current-buffer)))))
