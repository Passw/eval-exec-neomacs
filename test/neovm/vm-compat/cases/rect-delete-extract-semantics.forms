(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (delete-extract-rectangle 1 9) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (delete-extract-rectangle 8 7) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (delete-extract-rectangle 7 8) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (list (delete-extract-rectangle 20 1) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (list (delete-extract-rectangle 8 10) (string-to-list (buffer-string))))
(condition-case err (delete-extract-rectangle "x" 2) (error (car err)))
(condition-case err (delete-extract-rectangle 1 "x") (error (car err)))
