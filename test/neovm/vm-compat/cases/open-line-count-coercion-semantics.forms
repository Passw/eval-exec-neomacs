; open-line count coercion contract subset.
(list
 (with-temp-buffer
   (condition-case err (open-line -1) (error err)))
 (with-temp-buffer
   (insert "ab")
   (goto-char 2)
   (open-line 2)
   (list (point) (string-to-list (buffer-string))))
 (with-temp-buffer
   (insert "ab")
   (goto-char 2)
   (open-line 2.0)
   (list (point) (string-to-list (buffer-string))))
 (with-temp-buffer
   (insert "ab")
   (goto-char 2)
   (open-line -2.5)
   (list (point) (string-to-list (buffer-string)))))
