(with-temp-buffer
  (insert "abc")
  (goto-char 2)
  (list (yank-rectangle) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (kill-rectangle 1 9)
  (goto-char 1)
  (yank-rectangle)
  (list (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (kill-rectangle 1 9)
  (goto-char 2)
  (yank-rectangle)
  (list (point) (string-to-list (buffer-string))))
(condition-case err (yank-rectangle 1) (error (car err)))
