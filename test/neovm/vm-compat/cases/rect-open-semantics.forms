(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (open-rectangle 1 9) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (open-rectangle 2 10) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (open-rectangle 8 7) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (list (open-rectangle 20 1) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (goto-char 1)
  (list (open-rectangle 2 2) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (list (open-rectangle -5 2) (point) (string-to-list (buffer-string))))
(condition-case err (open-rectangle "x" 2) (error (car err)))
(condition-case err (open-rectangle 1 "x") (error (car err)))
