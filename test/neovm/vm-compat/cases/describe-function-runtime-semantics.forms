(stringp (describe-function 'car))
(condition-case err (describe-function 'vm-desc-fn-missing) (error (car err)))
(progn (fset 'vm-desc-fn (lambda nil 1)) (stringp (describe-function 'vm-desc-fn)))
(not (null (string-match-p "special-form" (downcase (describe-function 'if)))))
(progn (autoload 'vm-desc-fn-auto "files" "doc" nil nil) (not (null (string-match-p "lisp function" (downcase (describe-function 'vm-desc-fn-auto))))))
(progn (autoload 'vm-desc-fn-auto-m "files" "doc" nil 'macro) (not (null (string-match-p "lisp macro" (downcase (describe-function 'vm-desc-fn-auto-m))))))
(progn (defalias 'vm-desc-fn-alias 'car) (not (null (string-match-p "alias for" (downcase (describe-function 'vm-desc-fn-alias))))))
(progn (defalias 'vm-desc-fn-alias-missing 'vm-desc-fn-missing-target) (not (null (string-match-p "which is not" (downcase (describe-function 'vm-desc-fn-alias-missing))))))
(progn (fset 'vm-desc-fn-kmacro "abc") (not (null (string-match-p "keyboard macro" (downcase (describe-function 'vm-desc-fn-kmacro))))))
(progn (fset 'vm-desc-fn-macro (cons 'macro (cons '(lambda nil "m" nil) nil))) (not (null (string-match-p "lisp macro" (downcase (describe-function 'vm-desc-fn-macro))))))
(condition-case err (describe-function 1) (error (car err)))
(condition-case err (describe-function) (error (car err)))
(let ((s (describe-function 'car)))
  (list
   (not (null (string-match-p "car is a primitive-function" s)))
   (null (string-match-p "Built-in function" s))))
(progn
  (fset 'vm-desc-fn-shape2 (lambda nil 1))
  (let ((s (describe-function 'vm-desc-fn-shape2)))
    (list
     (not (null (string-match-p "vm-desc-fn-shape2 is a interpreted-function" s)))
     (null (string-match-p "Lisp function" s)))))
(progn
  (autoload 'vm-desc-fn-auto-shape "files" "doc" nil nil)
  (let ((s (describe-function 'vm-desc-fn-auto-shape)))
    (list
     (not (null (string-match-p "autoloaded Lisp function" s)))
     (not (null (string-match-p "‘files.el’" s))))))
(progn
  (defalias 'vm-desc-fn-alias-shape 'car)
  (let ((s (describe-function 'vm-desc-fn-alias-shape)))
    (list
     (not (null (string-match-p "alias for ‘car’" s)))
     (null (string-match-p "`car`" s)))))
(progn
  (autoload 'vm-desc-fn-auto-el-shape "files.el" "doc" nil nil)
  (let ((s (describe-function 'vm-desc-fn-auto-el-shape)))
    (list
     (not (null (string-match-p "autoloaded Lisp function\\." s)))
     (null (string-match-p " in ‘files.el’" s)))))
(progn
  (autoload 'vm-desc-fn-auto-elc-shape "files.elc" "doc" nil nil)
  (let ((s (describe-function 'vm-desc-fn-auto-elc-shape)))
    (list
     (not (null (string-match-p "autoloaded Lisp function\\." s)))
     (null (string-match-p "files.elc" s))
     (null (string-match-p "files.elc.el" s)))))
(progn
  (autoload 'vm-desc-fn-auto-path-shape "/tmp/files" "doc" nil nil)
  (let ((s (describe-function 'vm-desc-fn-auto-path-shape)))
    (list
     (not (null (string-match-p "autoloaded Lisp function\\." s)))
     (null (string-match-p "/tmp/files" s))
     (null (string-match-p "/tmp/files.el" s)))))
(progn
  (autoload 'vm-desc-fn-auto-empty-shape "" "doc" nil nil)
  (let ((s (describe-function 'vm-desc-fn-auto-empty-shape)))
    (list
     (not (null (string-match-p "autoloaded Lisp function" s)))
     (null (string-match-p " in ‘" s)))))
(progn
  (autoload 'vm-desc-fn-auto-macro-t-shape "files.elc" "doc" nil t)
  (let ((s (describe-function 'vm-desc-fn-auto-macro-t-shape)))
    (list
     (not (null (string-match-p "autoloaded Lisp macro" s)))
     (null (string-match-p "autoloaded Lisp function" s))
     (null (string-match-p "files.elc" s))
     (null (string-match-p "files.elc.el" s)))))
(progn
  (autoload 'vm-desc-fn-auto-keymap-shape "files" "doc" nil 'keymap)
  (let ((s (describe-function 'vm-desc-fn-auto-keymap-shape)))
    (list
     (not (null (string-match-p "autoloaded keymap" s)))
     (null (string-match-p "autoloaded Lisp function" s))
     (not (null (string-match-p " in ‘files.el’" s))))))
