; xml.c — XML parsing (libxml2 required)
; libxml-available-p
(not (not (libxml-available-p)))

; xml-parse-html-region on simple HTML
(with-temp-buffer
  (insert "<p>hello</p>")
  (listp (libxml-parse-html-region (point-min) (point-max))))

; xml-parse-string
(listp (libxml-parse-xml-string "<root>text</root>"))
