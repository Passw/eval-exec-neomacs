# One symbol name per line.
# This allowlist is for intentional core parity drifts in builtin function-cell
# shape (`fboundp` + subr/non-subr + subr arity). Keep empty unless drift is
# explicitly documented and approved.
string-chop-newline
string-fill
string-limit
string-pad
