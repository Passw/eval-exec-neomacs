(let ((kill-ring nil))
  (condition-case err
      (x-clipboard-yank)
    (error err)))
(let ((kill-ring '("abc")))
  (condition-case err
      (x-clipboard-yank)
    (error err)))
(let ((kill-ring '("abc" "def")))
  (condition-case err
      (x-clipboard-yank)
    (error err)))
(let ((kill-ring '(1)))
  (condition-case err
      (x-clipboard-yank)
    (error err)))
(let ((kill-ring 1))
  (condition-case err
      (x-clipboard-yank)
    (error err)))
(let ((kill-ring "abc"))
  (condition-case err
      (x-clipboard-yank)
    (error err)))
(let ((kill-ring ["abc"]))
  (condition-case err
      (x-clipboard-yank)
    (error err)))
(condition-case err
    (x-clipboard-yank nil)
  (error err))
