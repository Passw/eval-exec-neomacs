(fboundp 'key-binding)
(fboundp 'command-remapping)

(with-temp-buffer
  (condition-case e
      (key-binding 1 nil nil 0)
    (error e)))

(with-temp-buffer
  (condition-case e
      (command-remapping nil 0)
    (error e)))
