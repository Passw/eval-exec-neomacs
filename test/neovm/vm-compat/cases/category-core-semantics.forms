; category.c — category operations
; make-category-set
(stringp (make-category-set ""))

; category-table-p
(category-table-p (category-table))
(category-table-p (standard-category-table))
(not (category-table-p nil))
(not (category-table-p 42))

; category-table / standard-category-table
(char-table-p (category-table))
(char-table-p (standard-category-table))

; make-category-table
(char-table-p (make-category-table))

; define-category / category-docstring
(with-temp-buffer
  (let ((ct (make-category-table)))
    (set-category-table ct)
    (define-category ?x "test category" ct)
    (stringp (category-docstring ?x ct))))

; get-unused-category
(let ((ct (make-category-table)))
  (characterp (get-unused-category ct)))
